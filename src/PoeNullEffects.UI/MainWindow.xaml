<Window x:Class="PoeNullEffects.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:PoeNullEffects.UI.ViewModels"
        xmlns:views="clr-namespace:PoeNullEffects.UI.Views"
        Title="PoeNullEffects" Width="900" Height="700"
        WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <DockPanel>
        <!-- GGPK Path Bar -->
        <Border DockPanel.Dock="Top" Padding="8" Background="#F0F0F0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Auto-detect row -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Auto-detect:" VerticalAlignment="Center" Margin="0,0,8,4"/>
                <StackPanel Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="4" Orientation="Horizontal" Margin="0,0,0,4">
                    <Button Content="PoE 1" Command="{Binding FindPoe1Command}" MinWidth="70" Padding="8,4" Margin="0,0,4,0"/>
                    <Button Content="PoE 2" Command="{Binding FindPoe2Command}" MinWidth="70" Padding="8,4"/>
                </StackPanel>

                <!-- Path row -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="GGPK Path:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding GgpkPath, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>
                <Button Grid.Row="1" Grid.Column="2" Content="Browse" Command="{Binding BrowseGgpkCommand}"/>
                <Button Grid.Row="1" Grid.Column="3" Content="Open" Command="{Binding OpenGgpkCommand}" FontWeight="Bold"/>
                <Button Grid.Row="1" Grid.Column="4" Content="Close" Command="{Binding CloseGgpkCommand}" IsEnabled="{Binding IsGgpkOpen}"/>
            </Grid>
        </Border>

        <!-- Status Bar -->
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem Content="{Binding StatusText}"/>
            <Separator/>
            <StatusBarItem Content="{Binding StatsText}" FontWeight="SemiBold"/>
            <StatusBarItem HorizontalAlignment="Right">
                <Button Content="Launch PoE" Command="{Binding LaunchPoeCommand}"
                        Padding="16,4" MinWidth="120" FontWeight="Bold"/>
            </StatusBarItem>
        </StatusBar>

        <!-- Log Panel -->
        <Border DockPanel.Dock="Bottom" Height="140" BorderThickness="0,1,0,0" BorderBrush="Gray">
            <TextBox x:Name="LogBox" Text="{Binding LogText, Mode=OneWay}" FontFamily="Consolas" FontSize="11"
                     IsReadOnly="True" TextWrapping="NoWrap"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                     AcceptsReturn="True" TextChanged="LogBox_TextChanged"/>
        </Border>

        <!-- Tab Control -->
        <TabControl Margin="8" IsEnabled="{Binding IsGgpkOpen}">
            <TabItem Header="Null Effects">
                <views:NullEffectsView DataContext="{Binding NullEffects}"/>
            </TabItem>
            <TabItem Header="GGPK Browser">
                <views:GgpkBrowserView DataContext="{Binding GgpkBrowser}"/>
            </TabItem>
            <TabItem Header="Check List">
                <views:CheckListView DataContext="{Binding CheckList}"/>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
